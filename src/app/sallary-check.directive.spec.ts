import { SallaryCheckDirective } from './sallary-check.directive';

describe('SallaryCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new SallaryCheckDirective();
    expect(directive).toBeTruthy();
  });
});
